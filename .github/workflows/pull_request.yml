name: pull request

on:
  pull_request:
    branches:
      - main
    paths-ignore:
      - '*.md'

jobs:
  test:
    name: test
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
      with:
        submodules: recursive

    - uses: "actions/setup-python@v2"
      with:
        python-version: "3.7"

    - name: "Install system dependencies"
      run: sudo apt update -y && sudo apt install -y libssl-dev libasio-dev

    - name: "Install python environment"
      run: |
        python -m pip install black coverage flake8 mypy pytest
        # Because `pip install -e .` does not play nice with gcov, we go old school:
        python -m pip install -r build-requirements.txt
        CFLAGS="-coverage" python setup.py build_ext --inplace
        python setup.py develop
        cd vroom/src
        make python-coverage

    - name: "Run tests"
      run: make test

    - name: "Upload python coverage"
      uses: codecov/codecov-action@v2
      with:
        token: ${{ secrets.codecov_token }}
        files: 'coverage/coverage.xml'
        flags: python
        fail_ci_if_error: true

    - name: "Upload binding coverage"
      uses: codecov/codecov-action@v2
      with:
        token: ${{ secrets.codecov_token }}
        files: 'coverage/src#_vroom.cpp##src#bind#*.gcov'
        flags: binding
        fail_ci_if_error: true

    - name: "Upload VROOM coverage"
      uses: codecov/codecov-action@v2
      with:
        token: ${{ secrets.codecov_token }}
        files: 'coverage/src#_vroom.cpp##vroom#src#*.gcov'
        flags: vroom
        fail_ci_if_error: true

    - name: Verify clean directory
      run: git diff --exit-code
